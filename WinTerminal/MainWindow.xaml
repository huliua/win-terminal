<Window x:Class="WinTerminal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:WinTerminal.Controls"
        Title="WinTerminal" Height="700" Width="1100"
        MinHeight="500" MinWidth="700"
        Background="{StaticResource BgDarkBrush}"
        WindowStartupLocation="CenterScreen"
        KeyDown="Window_KeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0" Background="{StaticResource TitleBarBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="10,0">
                    <Button x:Name="BtnTerminal" Content="Terminal" Style="{StaticResource TabButtonActive}"
                            Click="BtnTerminal_Click" Margin="0,0,5,0"/>
                    <Button x:Name="BtnClaude" Content="Claude Code" Style="{StaticResource TabButton}"
                            Click="BtnClaude_Click" Margin="0,0,5,0"/>
                    <Button x:Name="BtnCodex" Content="Codex GUI" Style="{StaticResource TabButton}"
                            Click="BtnCodex_Click"/>
                </StackPanel>

                <TextBlock Grid.Column="1" Text="WinTerminal" Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Row="1">
            <!-- Terminal View -->
            <controls:TerminalControl x:Name="TerminalView" Visibility="Visible"/>

            <!-- Claude Code View -->
            <controls:ClaudeControl x:Name="ClaudeView" Visibility="Collapsed"/>

            <!-- Codex GUI View -->
            <controls:CodexControl x:Name="CodexView" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>
